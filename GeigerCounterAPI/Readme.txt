
A simple REST service to calculate average particle counts over time.

By default, runs using a SQL Server Express LocalDB, which can be migrated to a full SQL Server instance for deployment.

See the http://localhost:55999/index.html for API documentation.

